apiVersion: apps/v1
kind: Deployment
metadata:
  name: dyco
  labels:
    app: dyco-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dyco-app
  template:
    metadata:
      labels:
        app: dyco-app
    spec:
      containers:
        - name: dyco-app
          image: quay.io/dyco/dyco
          envFrom:
            - secretRef:
                name: dyco-secrets
